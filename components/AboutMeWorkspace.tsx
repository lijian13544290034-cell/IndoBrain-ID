'use client';

import Link from 'next/link';
import { useEffect, useMemo, useState } from 'react';
import IndonesianAudioProvider from '@/components/IndonesianAudioProvider';
import IndonesianSpeechButton from '@/components/IndonesianSpeechButton';
import type { CatalogExperience } from '@/lib/experience-catalog';
import { readLearningProfile, subscribeProfile, toggleFavorite, type LearningProfile } from '@/lib/learning-profile';

const statusLabel = { pending: '待审核', accepted: '已接受', published: '已发布', rejected: '未采用' } as const;

function catalogItemForFavorite(favoriteId: string, catalog: CatalogExperience[]) {
  const id = favoriteId.startsWith('ID_TO_ZH:') ? favoriteId.split(':')[1] : favoriteId;
  return catalog.find((item) => item.id === id);
}

export default function AboutMeWorkspace({ catalog, total, direction }: { catalog: CatalogExperience[]; total: number; direction?: 'ZH_TO_ID' | 'ID_TO_ZH' }) {
  const [profile, setProfile] = useState<LearningProfile>(() => readLearningProfile());
  useEffect(() => { const sync = () => setProfile(readLearningProfile()); sync(); return subscribeProfile(sync); }, []);
  const favorites = useMemo(() => profile.favorites.map((id) => ({ favoriteId: id, item: catalogItemForFavorite(id, catalog) })).filter((entry): entry is { favoriteId: string; item: CatalogExperience } => Boolean(entry.item)), [profile.favorites, catalog]);
  async function copy(text: string) { if (navigator.clipboard) await navigator.clipboard.writeText(text); }
  const visibleSubmissions = profile.submissions.filter((item) => direction === 'ID_TO_ZH' ? item.module === 'chinese' : item.module !== 'chinese');
  return <IndonesianAudioProvider><main className="mx-auto min-h-screen w-full max-w-4xl px-5 pb-12 pt-10 sm:px-8 sm:pt-14"><Link href="/" className="text-sm text-stone-500 hover:text-stone-900">← Beranda（返回首页）</Link><header className="mt-7 rounded-2xl border border-stone-200 bg-stone-50 px-5 py-5"><p className="text-xs text-stone-400">IndoBrain</p><h1 className="mt-1 text-2xl font-semibold">Tentang Saya <span className="text-base font-normal text-stone-500">（关于我）</span></h1><p className="mt-2 text-sm leading-6 text-stone-500">只保存这台设备上的学习记录；暂不跨设备同步。</p></header><section className="mt-7 grid gap-3 sm:grid-cols-3"><article className="rounded-xl border border-stone-200 bg-white p-4 shadow-sm"><p className="text-xs text-stone-400">收藏</p><p className="mt-2 text-2xl font-semibold">{favorites.length}</p></article><article className="rounded-xl border border-stone-200 bg-white p-4 shadow-sm"><p className="text-xs text-stone-400">连续学习天数</p><p className="mt-2 text-2xl font-semibold">{profile.currentStreak} 天</p></article><article className="rounded-xl border border-stone-200 bg-white p-4 shadow-sm"><p className="text-xs text-stone-400">已完成 Experience</p><p className="mt-2 text-2xl font-semibold">{profile.completed.length} / {total}</p></article></section><section className="mt-8"><h2 className="text-lg font-semibold">收藏</h2>{favorites.length ? <div className="mt-4 grid gap-3 sm:grid-cols-2">{favorites.map(({ favoriteId, item }) => <article key={favoriteId} className="flex min-h-44 flex-col rounded-xl border border-stone-200 bg-white p-4 shadow-sm"><p className="text-[11px] font-medium text-stone-400">{item.id} · {item.module}（{item.category}）</p><Link href={item.href} className="mt-2 font-semibold hover:underline">{item.task}</Link><p className="mt-2 text-sm leading-6 text-stone-700">{item.indonesian}</p><div className="mt-auto flex flex-wrap gap-2 pt-4"><IndonesianSpeechButton text={item.indonesian} language={item.audioLanguage} compact /><button onClick={() => copy(item.indonesian)} className="min-h-8 rounded-lg border border-stone-300 px-2 text-xs font-medium hover:bg-stone-100">复制</button><button onClick={() => { toggleFavorite(favoriteId); setProfile(readLearningProfile()); }} className="min-h-8 rounded-lg border border-stone-300 px-2 text-xs font-medium hover:bg-stone-100">取消收藏</button></div></article>)}</div> : <p className="mt-4 rounded-xl border border-stone-200 bg-stone-50 p-4 text-sm leading-6 text-stone-500">还没有收藏。打开任意 Experience，点击“收藏”即可保存。</p>}</section><section className="mt-8"><h2 className="text-lg font-semibold">我的贡献</h2>{visibleSubmissions.length ? <div className="mt-4 space-y-3">{visibleSubmissions.map((submission) => <article key={submission.id} className="rounded-xl border border-stone-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><p className="font-medium">{submission.happened}</p><span className="shrink-0 text-xs text-stone-400">{statusLabel[submission.status]}</span></div><p className="mt-2 text-sm text-stone-600">想表达：{submission.wantedToSay}</p><p className="mt-2 text-xs text-stone-400">{new Date(submission.submittedAt).toLocaleDateString()} · {submission.experienceId}</p></article>)}</div> : <p className="mt-4 rounded-xl border border-stone-200 bg-stone-50 p-4 text-sm leading-6 text-stone-500">还没有提交场景。你可以在任意 Experience 页面点击“场景共创”。</p>}</section></main></IndonesianAudioProvider>;
}
