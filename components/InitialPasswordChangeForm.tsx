'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function InitialPasswordChangeForm({ learningDirection }: { learningDirection: 'ZH_TO_ID' | 'ID_TO_ZH' }) {
  const router = useRouter(); const [password, setPassword] = useState(''); const [confirmPassword, setConfirmPassword] = useState(''); const [showPassword, setShowPassword] = useState(false); const [message, setMessage] = useState(''); const [submitting, setSubmitting] = useState(false);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setMessage(''); if (password !== confirmPassword) return setMessage('两次输入的密码不一致。'); setSubmitting(true); const response = await fetch('/api/auth/change-password', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ newPassword: password, initialPasswordChange: true }) }); const data = await response.json() as { error?: string }; setSubmitting(false); if (!response.ok) return setMessage(data.error ?? '无法修改密码。'); router.push(learningDirection === 'ID_TO_ZH' ? '/chinese' : '/'); router.refresh(); }
  return <form onSubmit={submit} className="mt-7 space-y-4"><p className="rounded-xl bg-stone-50 px-3 py-2 text-sm text-gray-600">为了保护账号安全，请先设置新的个人密码。</p><label className="block text-sm font-medium">新密码<input required minLength={10} value={password} onChange={(event) => setPassword(event.target.value)} type={showPassword ? 'text' : 'password'} autoComplete="new-password" className="mt-2 w-full rounded-xl border border-stone-300 px-3 py-3" /></label><label className="block text-sm font-medium">确认新密码<input required minLength={10} value={confirmPassword} onChange={(event) => setConfirmPassword(event.target.value)} type={showPassword ? 'text' : 'password'} autoComplete="new-password" className="mt-2 w-full rounded-xl border border-stone-300 px-3 py-3" /></label><button type="button" onClick={() => setShowPassword((value) => !value)} aria-label={showPassword ? '隐藏密码 Hide password' : '显示密码 Show password'} className="text-sm text-gray-600 underline underline-offset-4">{showPassword ? '隐藏密码' : '显示密码'}</button>{message && <p role="alert" className="rounded-xl bg-red-50 px-3 py-2 text-sm text-red-700">{message}</p>}<button disabled={submitting} className="w-full rounded-xl bg-stone-900 px-4 py-3 text-sm font-semibold text-white disabled:opacity-50">{submitting ? '正在保存…' : '保存新密码并继续'}</button><p className="text-xs text-gray-400">密码至少 10 位，并包含字母和数字。</p></form>;
}
